# Replace <PC_IP> with your computer's LAN IPv4 address
# Ensure kubectl port-forward is running: kubectl port-forward svc/api-gateway -n kharseka 8080:80
EXPO_PUBLIC_API_BASE_URL=http://<PC_IP>:8080
